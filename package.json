{
  "name": "lenslink-fullstack",
  "version": "1.0.0",
  "description": "LensLink Photography Booking Platform with Backend API",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "cd backend && nodemon server.js",
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npm install",
    "serve": "node simple-server.js",
    "deploy": "npm run build && npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd backend && npm install"
  },
  "keywords": ["photography", "booking", "express", "mongodb", "railway"],
  "author": "SaiKumarReddy",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-validator": "^7.0.1",
    "multer": "^1.4.5-lts.1",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "validator": "^13.11.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
