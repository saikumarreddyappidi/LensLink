# Nixpacks configuration for Railway deployment

[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmd = "npm install --production"

[start]
cmd = "node server.js"
